# ORIONIS Website

Полнофункциональный сайт для Minecraft сервера с личным кабинетом, админ-панелью и системой привилегий.

## Возможности

### Для пользователей
- 📝 Регистрация и вход в аккаунт
- 👤 Личный кабинет с профилем
- 🎮 Привязка Minecraft username
- 🔐 Изменение данных профиля
- 📋 Просмотр своих привилегий

### Для администраторов
- 👥 Управление пользователями
- 🔑 Выдача и отзыв привилегий
- 👔 Изменение ролей пользователей
- ⚙️ Настройка информации сервера
- 📊 Статистика и аналитика
- 📝 Логирование всех действий

### Привилегии
- `can_whitelist` - Управление вайтлистом
- `can_ban` - Блокировка игроков
- `can_manage_server` - Управление сервером
- `can_edit_ranks` - Редактирование рангов

## Установка

### Требования
- Node.js (v14 или выше)
- npm или yarn

### Шаги установки

1. **Клонирование/Открытие проекта**
```bash
cd c:\Users\VIPAR\Desktop\dev\site
```

2. **Установка зависимостей**
```bash
npm install
```

3. **Настройка переменных окружения**
Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

Отредактируйте `.env`:
```
PORT=3000
JWT_SECRET=your_super_secret_key_change_this
NODE_ENV=development
```

4. **Запуск сервера**
```bash
npm start
```

Сервер запустится на `http://localhost:3000`

## Для разработки

Используйте nodemon для автоматической перезагрузки:
```bash
npm run dev
```

## Структура проекта

```
├── src/
│   ├── server.js           # Основной файл сервера
│   ├── database.js         # Инициализация базы данных
│   ├── middleware/
│   │   └── auth.js        # Middleware аутентификации
│   └── routes/
│       ├── auth.js        # Маршруты аутентификации
│       ├── users.js       # Маршруты пользователей
│       └── admin.js       # Маршруты админ-панели
├── public/
│   ├── index.html         # Главная страница
│   ├── css/
│   │   └── style.css      # Стили
│   └── js/
│       └── app.js         # JavaScript логика
├── data/                  # Папка для базы данных
├── package.json
└── README.md
```

## API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход

### Пользователи
- `GET /api/users/profile` - Получить профиль
- `PUT /api/users/profile` - Обновить профиль
- `GET /api/users/list` - Список пользователей (админ)

### Администратор
- `GET /api/admin/users` - Все пользователи
- `GET /api/admin/users/:id` - Пользователь и его привилегии
- `POST /api/admin/grant-privilege` - Выдать привилегию
- `DELETE /api/admin/revoke-privilege` - Отозвать привилегию
- `PUT /api/admin/users/:id/role` - Изменить роль
- `GET /api/admin/logs` - Логи действий
- `GET /api/admin/server-info` - Информация о сервере
- `PUT /api/admin/server-info` - Обновить информацию сервера

## Роли и права доступа

### User (Пользователь)
- Просмотр своего профиля
- Редактирование своего профиля
- Просмотр своих привилегий

### Moderator (Модератор)
- Все права пользователя
- Просмотр списка пользователей
- Просмотр логов

### Admin (Администратор)
- Все права модератора
- Управление пользователями
- Выдача/отзыв привилегий
- Управление сервером

## Первый администратор

При первом запуске создается администратор с ID 1. Используйте следующие учетные данные для входа:

```
Пользователь: admin
Пароль: admin123
Email: admin@minecraft.local
```

⚠️ **ВАЖНО**: После первого входа измените пароль!

## Безопасность

- Все пароли хешируются с помощью bcryptjs
- JWT токены для аутентификации (сроком на 7 дней)
- CORS защита
- Проверка ролей и привилегий на каждом запросе

## Лицензия

MIT

## Поддержка

Если у вас есть вопросы или предложения, создайте issue в репозитории.
