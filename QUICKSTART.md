# 🎮 ORIONIS Website - Инструкция по запуску

## Быстрый старт

### 1. Откройте папку в VS Code
Откройте VS Code и откройте папку проекта: `c:\Users\VIPAR\Desktop\dev\site`

### 2. Откройте встроенный терминал
- Нажмите `Ctrl + ~` (или `Ctrl + ё`)
- Или перейдите в меню `Terminal > New Terminal`

### 3. Установите зависимости (если еще не установлены)
```
npm install
```

### 4. Запустите сервер
```
npm start
```
или
```
node src/server.js
```

### 5. Откройте в браузере
Перейдите на: **http://localhost:3000**

---

## Тестовые данные для входа

После первого запуска создается администратор:

```
Логин: admin
Пароль: admin123
```

**⚠️ Важно: После входа измените пароль администратора!**

---

## Основные функции

### 👤 Для пользователей:
- ✅ Регистрация и вход
- ✅ Личный кабинет
- ✅ Привязка Minecraft username
- ✅ Просмотр своих привилегий

### 🛡️ Для администраторов:
- ✅ Управление пользователями
- ✅ Выдача привилегий
- ✅ Изменение ролей
- ✅ Управление сервером
- ✅ Просмотр логов

---

## API Endpoints (для тестирования в Postman/Insomnia)

### Регистрация
```
POST http://localhost:3000/api/auth/register
Content-Type: application/json

{
  "username": "test_user",
  "email": "test@example.com",
  "password": "password123",
  "minecraft_username": "TestPlayer"
}
```

### Вход
```
POST http://localhost:3000/api/auth/login
Content-Type: application/json

{
  "username": "test_user",
  "password": "password123"
}
```

### Получить профиль
```
GET http://localhost:3000/api/users/profile
Authorization: Bearer <YOUR_TOKEN>
```

---

## Решение проблем

### Сервер не запускается
1. Убедитесь, что Node.js установлен: `node --version`
2. Убедитесь, что npm установлен: `npm --version`
3. Проверьте, что установлены зависимости: `npm install`
4. Проверьте, что порт 3000 не занят другим приложением

### Ошибка "Cannot find module"
Переустановите зависимости:
```
npm install
```

### CORS ошибки
Они нормальные в процессе разработки. Фронтенд на том же хосте, поэтому проблем не будет.

---

## Структура проекта

```
├── src/
│   ├── server.js          - Главный файл сервера
│   ├── database.js        - Конфигурация БД SQLite
│   ├── middleware/auth.js - Проверка авторизации
│   └── routes/
│       ├── auth.js        - Аутентификация
│       ├── users.js       - Управление профилем
│       └── admin.js       - Админ-панель
├── public/
│   ├── index.html         - Главная страница
│   ├── css/style.css      - Стили
│   └── js/app.js          - Фронтенд логика
├── data/                  - Папка с БД (создается автоматически)
└── package.json           - Зависимости проекта
```

---

## Дополнительная информация

- **База данных**: SQLite (автоматически создается в папке `data/`)
- **Аутентификация**: JWT токены
- **Хеширование паролей**: bcryptjs
- **Фреймворк**: Express.js

---

## Поддержка

Если у вас возникли проблемы, проверьте консоль браузера (F12) на наличие ошибок.

**Удачи с вашим Minecraft сервером! ⛏️**
